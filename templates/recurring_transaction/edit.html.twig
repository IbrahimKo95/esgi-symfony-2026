{% extends 'base.html.twig' %}

{% block title %}Modifier la transaction recurrente - ExpenseManager{% endblock %}

{% block main %}
    <div class="max-w-md mx-auto">
        <h1 class="text-xl font-bold text-gray-900 mb-6">Modifier la transaction recurrente</h1>

        <div class="bg-white rounded-xl border border-gray-100 shadow-sm p-6">
            {{ form_start(form) }}
                {{ form_row(form.amount) }}
                {{ form_row(form.description) }}
                {{ form_row(form.wallet) }}
                {{ form_row(form.category) }}
                {{ form_row(form.frequency) }}
                {{ form_row(form.nextOccurrence) }}
                {{ form_row(form.isActive) }}
                <button type="submit" class="w-full bg-indigo-600 text-white py-2.5 rounded-lg font-medium hover:bg-indigo-700 transition">Enregistrer</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
