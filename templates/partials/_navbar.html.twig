<nav class="sticky top-0 z-10 bg-white/80 backdrop-blur border-b border-gray-200">
    <div class="max-w-4xl mx-auto px-4 h-16 flex justify-between items-center">
        <a href="{{ path('app_home') }}" class="flex items-center gap-2 font-bold text-gray-900">
            <span class="flex items-center justify-center w-8 h-8 rounded-lg bg-indigo-600 text-white">
                <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <rect x="2" y="6" width="20" height="14" rx="2"></rect>
                    <path d="M2 10h20"></path>
                    <circle cx="16" cy="14.5" r="1.5" fill="currentColor" stroke="none"></circle>
                </svg>
            </span>
            <span>ExpenseManager</span>
        </a>

        <div class="flex items-center gap-3 text-sm">
            {% if app.user %}
                <a href="{{ path('wallet_index') }}" class="text-gray-600 hover:text-gray-900 transition">Portefeuilles</a>
                <a href="{{ path('tag_index') }}" class="text-gray-600 hover:text-gray-900 transition">Tags</a>
                <a href="{{ path('recurring_transaction_index') }}" class="text-gray-600 hover:text-gray-900 transition">Recurrentes</a>
                <a href="{{ path('app_profile') }}" class="flex items-center gap-2 text-gray-600 hover:text-gray-900 transition">
                    <span class="flex items-center justify-center w-7 h-7 rounded-full bg-indigo-100 text-indigo-700 text-xs font-semibold">
                        {{ app.user.firstName|first|upper }}{{ app.user.lastName|first|upper }}
                    </span>
                    <span class="hidden sm:inline">Profil</span>
                </a>
                <a href="{{ path('app_logout') }}" class="text-gray-500 hover:text-gray-900 transition">Deconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="text-gray-600 hover:text-gray-900 transition">Connexion</a>
                <a href="{{ path('app_register') }}" class="bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700 transition">Inscription</a>
            {% endif %}
        </div>
    </div>
</nav>
